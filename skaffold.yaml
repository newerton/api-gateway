apiVersion: skaffold/v1
kind: Config
metadata:
  name: dev-k8s-api-gateway
build:
  local:
    push: false
  artifacts:
    - image: newerton/api-gateway
      context: .
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: "src/**/*.*"
            dest: .
